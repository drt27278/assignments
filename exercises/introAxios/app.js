// Axios - JavaScript library (node_module) that helps us make AJAX calls to APIs. //

    // 1. An event (page load).
    // 2. XMLHttp request object is created.
    // 3. The request object sends the request to the server. xhr.save().
    // 4. The server processes the request.
    // 5. The server sends a response back to the webpage.
    // 6. The respones is then read by JavaScript.

    // var axios = require('axios');

    // // Create - Posting new data (think signing up for the first time)
    // axios.get('http://swapi.co/api/people/1').then(function(response){
    //     console.log(response)
    // )}.catch(function(err){
    //     console.log(err)
    // })
    
    // });